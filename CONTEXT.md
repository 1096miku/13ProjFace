# 车载AI行车状态监测与语音交互终端

一台装在车内的 ESP32-S3 终端：靠板载 IMU 判断车在做什么，靠摄像头与环境传感器看车内状况，用屏幕与语音与人交互，联网后把事件送到手机。设备同时是小智AI 语音助手。

## 行车与状态

**行车事件（Driving Event）**：
由 IMU 采样判定出来的一次驾驶行为事实，取值 急加速／急刹车／急转弯／颠簸／碰撞。只增不改，带时间戳与触发值。
_Avoid_：报警、告警（告警是事件触发的动作，不是事件本身）

**车辆状态（Motion State）**：
设备对"车现在处于什么状态"的判断，取值 标定中／停车／行驶／锁车监测。
_Avoid_：行车模式、工作模式

**静止基线（Static Baseline）**：
设备通电后静止时标定出的三轴零点。所有阈值都判定在"相对基线的变化量"上，而不是绝对加速度——这样安装方向偏差不会让判定失效。

**锁车（Lock）**：
进入锁车监测模式的动作。停车持续到配置时长会自动触发，也可由屏幕按钮或语音手动触发。它是**设备的监测状态**，不是车辆的中控锁。

**锁车监测模式（Locked Monitor Mode）**：
车已停放、设备转入看守的状态。此状态下的震动会被当作异常而不是普通颠簸。

**异常震动（Motion While Parked）**：
锁车监测模式下的加速度变化超阈事件。它是"可能有人遗留"的信号来源，但**不等于结论**。

## 车内环境

**环境数据（Environment Reading）**：
一次车内温度／湿度／光照的采样，带时间戳与来源标记（真实／模拟）。

**环境数据源（Environment Source）**：
提供环境数据的实现。当前只有模拟源，传感器接入后增加真实源，上层不变。

**模拟源（Simulated Source）**：
没有硬件时使用的数据源，按基线加缓变生成数据。界面、上报、语音查询三条链路照常真实运行，只是数据来源不同。
_Avoid_：假数据（链路是真的，只有来源不同）

## 遗留监测

**锁车抓拍（Lock-time Snapshot）**：
进入锁车监测时抓取的一张车内画面；锁车态每次异常震动再抓一张。

**遗留确认（Occupancy Confirmation）**：
由人在手机端看着抓拍画面作出的"车内是否有人遗留"的判断。**设备不做这个判断**——这是本项目的明确边界。

## 语音

**命令词（Command Word）**：
ESP-SR 离线识别的固定词条，本设备共 8 条。词表是固定的，改词表要重新出模型。

**播报片段（Prompt Clip）**：
编入固件的预录音频，是播报的最小单位。设备**没有 TTS**，所以"温度 26 度"是片段加数字音拼出来的，不是实时合成。

**车载问答（In-car Q&A）**：
由小智AI 云端提供的自由问答，需要联网。

## 通道与角色

**事件通道**：
设备把行车事件与状态推给巴法云 MQTT 的通道，外网可订阅查看。

**原图通道**：
设备在局域网内用 HTTP 提供最近一张抓拍原图的通道，需与设备同一 Wi-Fi。

**小智AI（XiaoZhi）**：
设备端的语音助手能力，由 xiaozhi 固件加上其对话服务端共同提供；负责自由问答，不参与行车判定。
