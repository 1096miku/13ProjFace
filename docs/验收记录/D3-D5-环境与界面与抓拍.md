# D3–D5 验收记录（环境数据 + 四页界面 + 抓拍与实时画面）

- 日期：**D3 = 2026-09-17 晚**（上一轮会话）；**D4 = 2026-09-17 晚 ~ 2026-09-18 上午**（本轮）
- 仓库：`D:\vscode\ESP32Project\13ProjFace`，分支 `main`；基线 HEAD `fbd87ab`，本轮 D4 改动**尚未提交**（文件清单见文末）
- 固件版本：App `xiaozhi` 2.2.4 / ESP-IDF `v5.5.3`；`build/xiaozhi.elf` SHA256 `AE163003B33028C09B2F8E683C839E8ADE339C35A4DC53256F19687F5D4928CD`；`xiaozhi.bin` = 3,044,080 B（ota_0 余 26%）
- 硬件：自定义 ESP32-S3 开发板（N16R8 / 480×320 / GC0308 / QMI8658A / PCA9557）
- 串口：D3 = `COM13`；D4 = `COM13` 与 `COM10`（同一块板的两个口，本轮 USB 反复重新枚举，见 §6）
- 证据文件（都在 `build/`，gitignored）：
  - `acceptance_d3b.log` —— D3 真机（含 BUG-006 触摸 NACK 现场，332.65 s 处）
  - `acceptance_d4_bug024_crash.log` —— D4 第一版：SPIFFS 断言复位 **13 次**
  - `acceptance_d4_bug025_stuck.log` —— 预览卡死版（首版，13.6 fps + 8 张抓拍 + 4 次小智拍照成功）
  - `acceptance_d4_bug025_stall.log` —— `CAMERA_GRAB_LATEST` 版：预览掉到 9.8 fps + 驱动**永久停摆**
  - `acceptance_d4_bug025_v3hang.log` —— `fb_count=3`+`LATEST` 版：`EV-EOF-OVF` / `FB-SIZE` + 小智拍照上传挂死
  - `acceptance_d4e.log` —— **D4 最终版**（本节数据来源）
- **D5 部分（任务 10–12：HTTP 三路由、设置页读数 + 4 个 MCP 工具、带负载稳定性）本轮未做**，本文件先把 D3 + D4 落下来

---

## 1. 主机单元测试（D4 收尾回归）

七套全部通过（`C:\mingw64\bin` 在 PATH 上，`-Wall -Wextra` 零 warning）：

| 套件 | 结果 |
|---|---|
| `driving_monitor_test` | `全部通过（失败 0 项）` |
| `imu_convert_test` | `all passed` |
| `event_history_test` | `all passed` |
| `environment_sim_test` | `all passed` |
| `snapshot_ring_test` | `all passed`（19 项，D4 新增） |
| `event_json_test` | `all passed` |
| `event_text_test` | `all passed` |

---

## 2. D3 验收（环境数据 + 四页界面）

> 数据来自上一轮会话的真机验收（交接文档 `docs/handoff/2026-09-17-planB-d3-completed-handoff.md`）与 `build/acceptance_d3b.log`；本轮只复核了日志里的内存与故障行。

| 项 | 期望 | 实测 |
|---|---|---|
| 开机与界面创建 | 4 页 + 入口按钮 | ✅ `车载界面定时器已创建（1000 ms 刷新 / 60 ms 预览）` → `车载界面已创建（4 页 + 聊天界面上的入口按钮）` |
| 中文字体 | 两段贴字体日志，四页中文无缺字 | ✅ `已套用主题字体` 出现**两次**（2.0 s basic → 7.0 s `assets.Apply()` 后 common），用户确认四页中文正常 |
| 主页 | 温度/湿度/光照 + 行车状态 + 事件数 + 联网图标 | ✅ 模拟源每秒在变 |
| 事件页翻页 | 上一页/下一页有效 | ✅ 修完 BUG-019（加 5 键导航栏）后可用 |
| 导航栏 | 主页/画面/事件/设置/返回 都可按 | ✅ |
| 设置页排版 | 不重叠、不出屏 | ✅ 修完 BUG-021 后用户确认 |
| 锁车监测 → 行驶 | 能回到行驶 | ✅ 修完 BUG-020 后用户确认 |
| 内部 RAM（UI 运行中） | ≥10 KB | ✅ `free sram: 28315~28807` / `minimal sram: 14759` |
| 整机稳定性 | 不重启 | ⚠️ **未达成**：uptime **332.65 s** 时触摸 I2C 一次 NACK → 上游 `ESP_ERROR_CHECK` abort（BUG-006，用户决定暂不修） |

---

## 3. D4 验收（抓拍落盘 + 实时画面 + 与小智拍照共存）

### 3.1 存储底座（任务 7）

| 项 | 实测 |
|---|---|
| 分区表 | `snapshots, data, spiffs, 0xdc0000, 2304K`（删掉了 `human_face_det`/`human_face_feat`，其余偏移未动；`gen_esp32part.py` 解出的表已核对） |
| 挂载 | ✅ `snapshots 已挂载到 /snap：共 2110 KB，已用 0 B（挂载耗时 92 ms）` |
| 首次挂载 | 擦空分区后重启，**仍直接挂载成功（未触发格式化）**，耗时 92–95 ms —— `format_if_mount_failed` 这条路径本板没被走到 |
| 事件日志 | `events.log` 由 `SnapshotStore::OnEvent` 追加；`/events` 的读取验证留给 D5（本轮没有 HTTP） |
| 抓拍环形 | 32 槽位，文件名 `snap_000.jpg`…；本轮两版固件累计写出 **11 张**（首版 snap_000~007，最终版 snap_000~003，见 §3.3） |

### 3.2 实时画面页（任务 9）

| 项 | 判据 | 实测 |
|---|---|---|
| 画面出图 | QVGA 320×240，**不做放大** | ✅ 用户确认画面实时（不卡） |
| 实测帧率 | ≥10 fps | **13.1–15.0 fps**（`预览实测 14.8 fps（75 帧 / 5.0 s，丢帧 0）` 一类），**长跑后段降到 10.0 fps** |
| 采用的定时器周期 | —— | `kPreviewIntervalMs = 60 ms`（**没有**按计划降级到 100 ms；后段的 10.0 fps 是 LVGL 刷 150 KB 画布吃满导致的节拍滑落，不是取帧失败——`丢帧 0`） |
| 对话期间行为 | 计划要求"非待机态暂停预览" | ⚠️ **刻意偏离**：改为**持续预览**（暂停会让驱动停摆，且用户明确要求画面一直实时）。日志证据：唤醒/说话全程仍有 `预览实测` 行，抓拍的瞬间从 13.6 掉到 11.8 fps 后自行恢复 |
| 摄像头不可用提示 | 连续失败要上屏 | ✅ 该路径在"停摆版"固件里真触发过（`连续 30 次取帧失败，预览页显示"摄像头不可用"`），最终版没再触发 |

### 3.3 抓拍（任务 8）

| 路径 | 实测 |
|---|---|
| 屏幕「抓拍」×3 | ✅ `snap_000.jpg`(8328 B) / `snap_001.jpg`(8486 B) / `snap_002.jpg`(8356 B)，全部 `落盘=是` |
| 进「锁车监测」自动抓拍 | ✅ `抓拍完成：原因=进入锁车监测 6 KB ts=122.262 s 落盘=是`（首版另有 `原因=异常震动 7 KB` 的抓拍） |
| 抓拍失败的处理 | 只 `ESP_LOGW`、不往事件历史塞条目（按计划偏离表第 8 条） |
| worker 栈余量 | 每次抓拍打印 `worker 栈余量 6088 B`（栈 8192 B，在**内部 RAM**，见 BUG-024） |
| `self.camera.take_photo` 共存 | ✅ `Captured frame: 320x240` → `JPEG encoding time: 45 ms` → `Explain image size=320x240, compressed size=9322, question=抓拍当前画面`（服务端返回了分析结果，用户确认小智能读出图片内容） |

### 3.4 带负载连续运行

| 项 | 实测 |
|---|---|
| 连续预览时长 | **588 s**（`acceptance_d4e.log` 的 fps 行连续输出到 613 s 那次截止） |
| 取帧失败 | **0 次**（无 `Failed to get frame: timeout`、无 `取帧失败累计`） |
| 驱动告警 | `cam_hal: EV-VSYNC-OVF` **2 次**，均自恢复，未停摆 |
| 内部 RAM | 稳态 `free sram: 17.8~19.4 KB`；**`minimal sram: 1003 B`**（见 §5 局限） |
| 重启 | 无固件自发重启；日志里的 `rst:0x15 (USB_UART_CHIP_RESET)` 是抓取脚本在 USB 掉线重连时拉 RTS 造成的（见 §6） |

---

## 4. D4 期间定位到的三个缺陷（细节见 `docs/BUGS.md`）

| 编号 | 一句话 | 状态 |
|---|---|---|
| BUG-022 | 计划里 `snapshot_ring` 的测试夹具自相矛盾（容量 3 的环断言槽位 7/31 有文件名） | 已修（改用容量 32 的环） |
| BUG-023 | 计划给的两段代码在 ESP-IDF 上编不过（`-Werror=format-truncation`；`CameraCapture` 因 `EventSink::OnEvent` 是纯虚成了抽象类）——**主机测试全绿也编不过** | 已修 |
| BUG-024 | **worker 任务（栈在 PSRAM）里写 SPIFFS → `assert(esp_task_stack_is_sane_cache_disabled())` 复位**，点抓拍/锁车/晃板子必崩（复现 13 次） | 已修（worker 栈改内部 RAM） |
| BUG-025 | 预览与小智拍照抢同一颗 GC0308：`CAMERA_GRAB_WHEN_EMPTY` + 上游 `Capture()` 长期持帧 + "对话暂停预览" → 预览卡死、驱动**永久停摆** | 已修（不去暂停 + `try_lock` + 节流日志；相机参数保持计划原样） |

---

## 5. 局限（如实记录）

1. **环境数据是模拟源**，不是真实传感器（本板 I2C 地址已占满，计划书 §11 已列为已知限制）；界面上的"模拟"标注是刻意保留的。
2. **无 RTC**：事件时间只有"开机以来的毫秒"，页面与 JSON 里都不写"今日"。
3. **抓拍与预览分辨率都是 QVGA 320×240**，抓拍 JPEG 约 6–11 KB。
4. **帧率是 10–15 fps，不是稳定的 16 fps**：瓶颈是 LVGL 把 320×240 RGB565 画布经 SPI 刷到屏（约 30 ms/帧），不是相机或取帧。计划要求的 ≥10 fps 在典型情况下满足，长跑后段会贴到 10.0。
5. **内部 RAM 低水位只剩 1003 B**：`free sram` 稳态 17.8~19.4 KB，但对话路径（TLS + JPEG 编码线程）瞬时会把内部堆压到 1 KB。**D5 要加 HTTP 任务（计划给 6 KB 内部栈）之前必须重新评估**——可用的手段：把 `kWorkerStackBytes` 从 8192 降到 6144（实测最坏只用约 2.1 KB）、减少 SPIFFS `max_files`、或把 HTTP 任务做小。**不要**把会碰 flash 的任务栈放到 PSRAM（BUG-024）。
6. **BUG-006（触摸 I2C 一次 NACK → 整机 abort）未修**：D3 在 332.65 s 复现过。它让"连续 2 小时不重启"这类指标目前不可能达成（用户 2026-09-17 决定暂不修）。
7. **相机与预览的共存仍然脆弱**：本轮已把两个明确根因修掉（BUG-024/025），但 `EV-VSYNC-OVF` 仍偶发（588 s 里 2 次，能自恢复），`PSRAM DMA mode disabled` 下驱动每帧要软件搬 153,600 B 这个负载源没有消除。
8. **小智拍照的上传出现过一次挂死**（`fb_count=3`+`LATEST` 那一版，`JPEG encoding time` 都没打出来，`Wait for HTTP headers receive timeout` 两次后失败）。最终版这一路径实测正常（§3.3），但**根因未定位**，若后续再出现，先查当时内部 RAM 余量与 `HttpClient`/`Encoder` 线程的分配情况。

---

## 6. 环境与操作要点（本轮踩到的）

1. **串口号会跟着 USB 重新枚举跑**：本轮同一块板依次出现在 `COM13 → COM12 → COM13 → COM10/COM13 → (全部消失) → COM10+COM13`。抓取脚本**必须遍历候选端口**，写死一个必然漏掉开机日志（BUG-018）。两个口都在同一块板上（MAC `e8:3d:c1:fb:a2:60`）。
2. **抓取脚本在 USB 掉线重连时会拉 RTS 把板子复位**：日志里 `rst:0x15 (USB_UART_CHIP_RESET)` + `### port error ... I/O operation has been aborted` 成对出现，**不是固件崩溃**，别误判。
3. `idf.py` 在本会话的沙箱下必须放宽权限（`danger-full-access`），否则 `PermissionError: [WinError 5]`（BUG-008）。
4. 烧录：改分区表后跑过一次 `idf.py flash`（会写 `0x8000`）；之后只改 app 时用 `idf.py -p <PORT> app-flash` 更快。
5. **构建要确认日志里真有新编译的 `.obj` 行**：`reconfigure` 失败时 `&&` 会让 `build` 根本不执行，而 `last_build.log` 留着上一轮的旧内容。
6. 复位优先"拔插 USB"，不要按 RST（BUG-005：复位落在 I2C 事务中间会把总线钳死）。

---

## 7. D4 改动的文件清单（`git status --short`）

```
 M docs/BUGS.md                       # BUG-022 / 023 / 024 / 025
 M main/CMakeLists.txt                # SOURCES += vehicle/snapshot_ring.cc
 M main/boards/esp32s3/camera_capture.{h,cc}   # SnapshotStore 落盘、EventSink、try_lock、取帧失败日志、栈余量日志
 M main/boards/esp32s3/esp32s3_board.cc        # snapshots 挂载、CameraCapture 接线、fb_count=2、VehicleUi 传相机
 M main/boards/esp32s3/vehicle_service.cc      # worker 栈改内部 RAM（BUG-024）
 M main/boards/esp32s3/vehicle_ui.{h,cc}       # 预览不暂停、摄像头不可用提示
 M partitions/v2/16m.csv              # 删两个人脸模型分区，建 snapshots(0xdc0000, 0x240000)
?? main/boards/esp32s3/snapshot_store.{h,cc}
?? main/vehicle/snapshot_ring.{h,cc}
?? test/snapshot_ring_test.cc
```
